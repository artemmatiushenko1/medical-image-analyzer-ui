import { createStyleSheet } from '@/libs/theme';

const styles = createStyleSheet({
  root: {
    gap: 1,
  },
  imageUploadHints: {
    display: 'flex',
    justifyContent: 'space-between',
  },
});

export { styles };
